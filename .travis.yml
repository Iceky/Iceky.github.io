language: node_js

sudo: false

branches:
  only:
  - hexo

before_install:
- chmod 600 ~/.ssh/id_rsa
- eval $(ssh-agent)
- ssh-add ~/.ssh/id_rsa
- cp .travis/ssh_config ~/.ssh/config

install:
- npm install hexo-cli -g
- npm install

script:
- hexo clean
- hexo g
- hexo d

